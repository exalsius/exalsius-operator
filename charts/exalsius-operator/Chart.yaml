apiVersion: v2
name: exalsius-operator
description: A Helm chart for the exalsius operator.
type: application
# version and appVersion are kept in lockstep and bumped by release-please
# (see release-please-config.json extra-files).
version: 0.8.3
appVersion: "0.8.3"
