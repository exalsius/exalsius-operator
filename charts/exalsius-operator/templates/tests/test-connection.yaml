apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "exalsius-operator.fullname" . }}-test-connection"
  labels:
    {{- include "exalsius-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "exalsius-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
