---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: colonies.infra.exalsius.ai
spec:
  group: infra.exalsius.ai
  names:
    kind: Colony
    listKind: ColonyList
    plural: colonies
    singular: colony
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyClusters
      name: Ready Clusters
      type: integer
    - jsonPath: .status.totalClusters
      name: Total Clusters
      type: integer
    name: v1
    schema:
      openAPIV3Schema:
        description: Colony is the Schema for the colonies API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ColonySpec defines the desired state of Colony.
            properties:
              colonyClusters:
                description: ColonyClusters is the list of clusters to create.
                items:
                  properties:
                    clusterDeploymentSpec:
                      description: |-
                        ClusterDeployment is the specification for the cluster deployment.
                        This is stored as RawExtension to avoid CRD validation issues with k0rdent types
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    clusterLabels:
                      additionalProperties:
                        type: string
                      description: ClusterLabels are the labels to be applied to the
                        ClusterDeployment resource
                      type: object
                    clusterName:
                      description: Name is the name of the cluster.
                      type: string
                  required:
                  - clusterName
                  type: object
                type: array
              netBird:
                description: NetBird configuration for VPN networking
                properties:
                  apiKeySecret:
                    description: |-
                      APIKeySecret is the name of the Secret containing the NetBird API key.
                      The secret must exist in the "exalsius-system" namespace.
                    type: string
                  enabled:
                    description: Enabled enables NetBird integration for this Colony.
                    type: boolean
                  image:
                    description: 'Image is the NetBird container image to use (default:
                      "netbirdio/netbird:v0.60.2").'
                    type: string
                  managementURL:
                    description: 'ManagementURL is the NetBird management API URL
                      (default: "https://api.netbird.io").'
                    type: string
                required:
                - apiKeySecret
                - enabled
                type: object
            type: object
          status:
            description: ColonyStatus defines the observed state of Colony.
            properties:
              clusterDeploymentRefs:
                items:
                  description: ObjectReference contains enough information to let
                    you inspect or modify the referred object.
                  properties:
                    apiVersion:
                      description: API version of the referent.
                      type: string
                    fieldPath:
                      description: |-
                        If referring to a piece of an object instead of an entire object, this string
                        should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                        For example, if the object reference is to a container within a pod, this would take on a value like:
                        "spec.containers{name}" (where "name" refers to the name of the container that triggered
                        the event) or if no container name is specified "spec.containers[2]" (container with
                        index 2 in this pod). This syntax is chosen only to have some well-defined way of
                        referencing a part of an object.
                      type: string
                    kind:
                      description: |-
                        Kind of the referent.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                      type: string
                    name:
                      description: |-
                        Name of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                    namespace:
                      description: |-
                        Namespace of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                      type: string
                    resourceVersion:
                      description: |-
                        Specific resourceVersion to which this reference is made, if any.
                        More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                      type: string
                    uid:
                      description: |-
                        UID of the referent.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                type: array
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              netBird:
                description: NetBird status for VPN networking
                properties:
                  clusterResources:
                    additionalProperties:
                      description: ClusterNetBirdStatus tracks NetBird resources for
                        a specific cluster.
                      properties:
                        controlPlaneResourceAddress:
                          description: ControlPlaneResourceAddress is the last known
                            address of the resource (for migration/debugging).
                          type: string
                        controlPlaneResourceID:
                          description: ControlPlaneResourceID is the NetBird Network
                            Resource ID for the control plane.
                          type: string
                        exposedEndpoint:
                          description: |-
                            ExposedEndpoint is the address workers use to join the cluster.
                            Format: "<ip>:<port>" or "<hostname>:<port>"
                          type: string
                        externalAddress:
                          description: ExternalAddress is the LoadBalancer external
                            address (if applicable)
                          type: string
                        ready:
                          description: Ready indicates whether this cluster's NetBird
                            resources are ready.
                          type: boolean
                        serviceName:
                          description: ServiceName is the control plane service name.
                          type: string
                        serviceNamespace:
                          description: ServiceNamespace is the control plane service
                            namespace.
                          type: string
                        serviceType:
                          description: ServiceType is either "LoadBalancer" or "NodePort".
                          type: string
                        useDirectConnection:
                          description: |-
                            UseDirectConnection indicates if workers connect directly (true for LB)
                            or via NetBird routing peer (false for NodePort)
                          type: boolean
                      type: object
                    description: ClusterResources tracks NetBird resources per cluster.
                    type: object
                  colonyMeshPolicyID:
                    description: ColonyMeshPolicyID is the NetBird Policy ID for the
                      shared colony-scoped mesh policy.
                    type: string
                  colonyNodesGroupID:
                    description: ColonyNodesGroupID is the NetBird Group ID for the
                      shared colony-scoped nodes group.
                    type: string
                  colonyRoutersGroupID:
                    description: ColonyRoutersGroupID is the NetBird Group ID for
                      routing peers only.
                    type: string
                  networkID:
                    description: NetworkID is the NetBird network ID.
                    type: string
                  routerReady:
                    description: RouterReady indicates whether the routing peer Deployment
                      is ready.
                    type: boolean
                  routerSetupKeyID:
                    description: RouterSetupKeyID is the NetBird setup key ID for
                      the routing peer.
                    type: string
                  routerSetupKeySecretName:
                    description: RouterSetupKeySecretName is the name of the Secret
                      containing the router-specific setup key.
                    type: string
                  setupKeyID:
                    description: SetupKeyID is the NetBird setup key ID for tracking/regeneration.
                    type: string
                  setupKeySecretName:
                    description: SetupKeySecretName is the name of the auto-generated
                      Secret containing the setup key.
                    type: string
                  trackedPeerIDs:
                    description: TrackedPeerIDs are peer IDs created by the operator
                      that should be cleaned up on deletion.
                    items:
                      type: string
                    type: array
                type: object
              phase:
                type: string
              readyClusters:
                format: int32
                type: integer
              totalClusters:
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
