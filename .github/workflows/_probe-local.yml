name: _probe-local (same-repo reusable test)

# Throwaway: confirms whether THIS repo can call a reusable workflow at all
# (same-repo, no cross-repo access settings involved). Delete after testing.

on:
  workflow_call: {}

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - run: echo "same-repo reusable workflow resolved OK"
